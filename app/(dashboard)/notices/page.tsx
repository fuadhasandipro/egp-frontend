'use client';

import { useEffect, useState } from 'react';
import apiClient from '@/lib/axios';

const demo = [{ id: 1, title: 'Monthly education coordination meeting', content: 'All head teachers are requested to attend the monthly coordination meeting at the Upazila office.', date: '14 Sep 2026', tag: 'Meeting' }, { id: 2, title: 'Mid-term assessment guidelines', content: 'The updated assessment guidelines are now available for all institutions under your jurisdiction.', date: '10 Sep 2026', tag: 'Guideline' }, { id: 3, title: 'Digital attendance reminder', content: 'Please ensure that attendance is checked in and reviewed daily before 10:00 AM.', date: '06 Sep 2026', tag: 'Reminder' }];

export default function NoticesPage() { const [notices, setNotices] = useState<any[]>(demo); useEffect(() => { apiClient.get('/api/notices?page=1&limit=20&sortBy=createdAt&order=DESC').then(res => { const data = res.data?.data?.items; if (Array.isArray(data) && data.length) setNotices(data); }).catch(() => undefined); }, []); return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Member 2 · Notices</p><h1 className="mt-2 text-3xl font-bold text-gray-900">Notices</h1><p className="mt-2 text-sm text-gray-500">Stay informed with circulars, guidance, and education updates.</p></div><section className="rounded-2xl border border-white bg-white/80 p-6 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Latest updates</p><h2 className="mt-1 text-xl font-bold">Notice board</h2><div className="mt-5 divide-y divide-gray-100">{notices.map(notice => <article key={notice.id} className="flex gap-4 py-5 first:pt-0"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-emerald-50 text-lg text-emerald-700">▤</div><div><div className="flex flex-wrap items-center gap-2"><h3 className="font-semibold text-gray-900">{notice.title}</h3><span className="rounded-full bg-gray-100 px-2 py-0.5 text-[10px] font-semibold text-gray-500">{notice.tag || 'Notice'}</span></div><p className="mt-1 text-sm leading-6 text-gray-500">{notice.content}</p><p className="mt-2 text-xs text-gray-400">Published {notice.date || (notice.createdAt ? new Date(notice.createdAt).toLocaleDateString() : 'recently')}</p></div></article>)}</div></section></div>; }
